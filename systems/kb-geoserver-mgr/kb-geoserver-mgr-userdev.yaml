apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{system}}-userdev
spec:
  replicas: 1
  strategy:
    type: Recreate  
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: apps.deployment-{{namespace}}-{{system}}-userdev
  template:
    metadata:
      labels:
        workload.user.cattle.io/workloadselector: apps.deployment-{{namespace}}-{{system}}-userdev	
    spec:
      securityContext:
        fsGroup: 20000                 # Automatically updates disk contents to group 20000
        supplementalGroups: [20000]
        fsGroupChangePolicy: "OnRootMismatch" # Skips chown on subsequent boots      
      initContainers:
        - name: folder-initializer
          image: ghcr.io/dbca-wa/docker-apps-dev:busybox_base_latest
          # Mount the raw, bare volume WITHOUT subpaths so we can fix the directories
          volumeMounts:
            - name: {{system}}-userdev
              mountPath: /mnt/storage/
          # We create the folders natively. Because fsGroup is active, 
          # these folders will inherit group 20000 access.
          command: ["sh", "-c", "mkdir -p /mnt/storage/data /mnt/storage/container /mnt/storage/data/data/projects/gis_kaartdijin_boodja_geoserver_manager/"]              
          securityContext:
            runAsNonRoot: true
            runAsUser: 200000   # Standard user, NOT root
            runAsGroup: 20000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            seccompProfile:
              type: RuntimeDefault            
            capabilities:
              drop:
                - ALL    
      tolerations:
        - effect: NoSchedule
          key: kubernetes.azure.com/scalesetpriority
          operator: Equal
          value: spot                   
      containers:
        - env:
            - name: ENABLE_WEB
              value: 'False'
            - name: ENABLE_CRON
              value: 'False'
          envFrom:
            - secretRef:
                name: {{system}}-{{namespace}}-userdev
                optional: false                
          name: {{system}}-userdev
          image: ghcr.io/dbca-wa/docker_app_dev:gis-kaartdijin-boodja-geoserver-manager_dev_latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: tcp8080
              protocol: TCP
          stdin: true          
          tty: true     
          volumeMounts:
            - mountPath: /data/
              name: {{system}}-userdev
              subPath: data                  
            - mountPath: /home/container/
              name: {{system}}-userdev
              subPath: container               
            - mountPath: /opt/geoserver_data/data/
              name: geoserver-2-27-azurefile-userdev              
          workingDir: /data/data/projects/gis_kaartdijin_boodja_geoserver_manager/
          resources:
            limits:
              cpu: '1'
              memory: 2Gi
            requests:
              cpu: 10m
              memory: 1Gi          
          securityContext:
            runAsNonRoot: true
            runAsUser: 200000   # Standard user, NOT root
            runAsGroup: 20000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            seccompProfile:
              type: RuntimeDefault            
            capabilities:
              drop:
                - ALL            
      volumes:
        - name:  {{system}}-userdev
          persistentVolumeClaim:
            claimName: {{system}}-userdev        
        - name: geoserver-2-27-azurefile-userdev
          persistentVolumeClaim:
            claimName: geoserver-2-27-azurefile-userdev               
            
